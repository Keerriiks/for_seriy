<h1>New Post</h1>

<%= form_for @post do |f| %>
  <div>
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>

  <div>
    <%= f.label :image %>
    <%= f.file_field :image %>
  </div>

  <%= f.submit 'Create Post' %>
<% end %>

<%= link_to 'Back', posts_path %>